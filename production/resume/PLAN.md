# Resumed Daybreak revision

Root owns the Sites checkout and integration. Use existing ITERATION-GATES.md as root acceptance ledger. User explicitly resumed development; historical transfer-only pause no longer applies.

Contract: preserve score attacks/pitches/velocities and character bind. Do not integrate scratch candidates without source hashes, actual mesh checks and visual review. Experimental scripts/assets must be returned outside checkout, no subagent Sites/browser lifecycle or checkout edits.

Leaves:
1. Arms and seated support: reconstruct +0.10m seat and constrained elbow pole; fixed world hand and shoe targets; torso/sleeve clearance, full-score reach and mixed wrist skin audit.
2. Hands: recover finite active/palm/refingering and idle thumb/nonthumb corrections, then inspect actual closeups and full intervals on integrated arms.
3. Audio: restore pinned sample bank, prepare exact final-score master pipeline; no changed-score stale-master acceptance.
4. Export/runtime: restore supported offline renderer, native browser capability check, final combined QA, then new native1080p30 full film.
5. Integration/publication: parent verifies each leaf, commits, public Sites version, asset/audio delivery and honest limitations.

No arbitrary elapsed-time waiting; meaningful work only. Preserve checkpoints in Git to avoid a second scratch loss.
