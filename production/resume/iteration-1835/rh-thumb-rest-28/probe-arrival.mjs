import fs from'node:fs';import{baseline,install,measure}from'./harness-pad.mjs';install(baseline);const rows=[];for(const ramp of[.04,.06,.08,.1])for(const lift of[.015,.025,.035,.045]){globalThis.__thumbRestCandidate={target:[.2,.45,.2],release:.4,arrival:.4,ramp,releaseLift:.015,arrivalLift:lift};let core=0,pairs=0,newKeys=0;for(let i=0;i<=150;i++){const r=measure(34.016094+i*.001,'R',{opposing:true});core=Math.max(core,...r.keyHits.filter(k=>k.patch==='RThumb').map(k=>k.depth));pairs+=r.crossings.filter(c=>c.a==='RThumb'||c.b==='RThumb').reduce((n,c)=>n+c.trianglePairs,0);}rows.push({ramp,lift,core,pairs,cost:Math.max(0,core-3)*100+pairs});}rows.sort((a,b)=>a.cost-b.cost);fs.writeFileSync('arrival-probes.json',JSON.stringify(rows,null,2));console.log(rows);
