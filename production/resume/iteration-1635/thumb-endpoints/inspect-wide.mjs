import fs from 'node:fs';import {baseline,install,measure,performer,T} from './harness.mjs';for(const [label,s] of [['baseline',baseline],['wide',JSON.parse(fs.readFileSync('candidate-wide-opposition.json'))]]){install(s);const r=measure(44.477,'R'),f=performer.hands[1].fingers[0];console.log(JSON.stringify({label,chain:r.chains[0],turn:f.bones.map((b,j)=>b.quaternion.angleTo(f.rest[j])),pairs:r.crossings.filter(c=>c.a==='RThumb'),mesh:r.meshContacts}));}
