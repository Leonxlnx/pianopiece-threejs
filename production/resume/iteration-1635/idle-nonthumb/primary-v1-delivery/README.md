# Primary score inactive-nonthumb additive candidate

This checkpoint combines107 selectively retained index support gaps, the bounded R3 approach pulse49.96–50.235, and the R4/R5 joint curves around162.1s. Apply the small `inactive-nonthumb.patch` and copy the two TS modules; do not replace the whole Pianist source.

Exact measured score:9db662f1e9d4037cc8c505b9818b7782ac1fb52fe70cff85bf362a00a9fb3b30. Exact measured source is arms+compact5,4ef26cd1df3d9b97f8a6e92378af4582fe2077e0cb5d6cc524cdb157891aea45. Run `node verify-idle-binding.mjs SCORE DATA_JSON RIG_TS input-manifest.json` before rebinding/integration; any score or rig mismatch requires the affected rechecks. Runtime gap IDs/times are an additional local guard, not a substitute for exact score validation.

Dense R3 target max core7.082→2.512mm. Dense R4 target max7.242→2.950mm, R5 max0; no newly colliding digit-pair type or severe core regression was observed in the full secondary gap or dense changed window. Actual active finger quaternions/tips remain exact apart from normalized-angle measurement floor; seek is deterministic.

Motion requires root review: the R4 changed-window peak tip speed is4.353m/s versus1.085m/s baseline. Current107+R3 whole-score peak is unchanged3.929m/s, but one local index speed rises.458m/s to.858m/s. No claim of fully natural motion is made.

The gap acceptance criterion concerns newly colliding digit-pair types. Existing pair intersection counts can increase; counts are not penetration depths. The107+R3 midpoint report preserves43 such rows and407 still-flagged gaps. Adding the R4 curve does not change the earlier source binding or erase those limitations. Finite sampling and owned mesh patches do not prove continuous whole-surface clearance. Root owns visual and full combined performer acceptance.

Root's E5 wrists89–91/p00196 correction and active-hand merged LH/RH revisions are separate pending score changes. They are not represented by this primary checkpoint. New E5/RH107 idle fits will be supplied as separately bound additions.
