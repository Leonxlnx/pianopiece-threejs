# Frozen RH107 inactive fan candidate

Add the three curves in `rh107-additive-curves.json` through the existing `applyIdleNonthumb` runtime API. No new runtime mode or held-note change is required. They are independently bound to source score5f3cb29e and must be rechecked before combining/rebinding the final score.

The requested RIndex/RMiddle collision during held p00537 (109.595–109.623047) is removed:143/143 observed colliding target poses become0 at5 kHz. The complete changed window109.5–109.74 is checked at1 kHz, all five actual finger surfaces against keys/neighbors/palm, with no newly colliding digit-pair type or severe core regression. Peak tip speed is unchanged1.792m/s. Active quaternion components/tips and deterministic seek remain exact apart from the normalized-angle measurement floor.

The correction coordinates released Middle/Ring/Pinky:20° MCP lift and−30° MCP spread, smooth ramps, bounded support109.52–109.72. Their independent gap IDs/times are retained in data. Across the complete union108.60832–120.484094,1529 finite poses include1492 outside the pulse; all joint quaternion components outside remain bit-identical. Combined with the changed-window surface audit, there is no newly observed deformation elsewhere in these same gaps.

This removes the requested held-neighbor failure; other preexisting idle pairs and thumb core excursions remain. Finite sampling and patch ownership do not establish continuous whole-body clearance or natural motion. Root owns final combined source/score and visual acceptance.
