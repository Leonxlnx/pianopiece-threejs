# Primary finite held-hand candidate

Frozen score: `candidate-primary-v1.json`, SHA-256 `9db662f1e9d4037cc8c505b9818b7782ac1fb52fe70cff85bf362a00a9fb3b30`. Baseline b156 and model77423 match `input-hashes.json`; compiled rig is the recovered field-capable f040 source, without later arm or inactive-thumb patches.

`primary-v1-manifest.json` contains all exact note and wrist-knot deltas. The three visibly reported distal-through-palm folds are repaired at sampled full held intervals: p00163 becomes L3, p00957 L3, p00741 R4. p00914 remains L2 with higher/deeper coherent wrist support. The preceding p00160/p00161 arrival was repaired without shortening holds; p00161 becomes L3. p00165 lift was recalibrated after the shared wrist support changed its skin contact.

Validation: `full-hold-validation.json` and the two full raw row files contain 1,859 poses each for baseline and candidate. Four complete windows use a120Hz grid, plus every intersecting note's full hold on its own120Hz grid, endpoints and quarter samples. There are308 primary-held poses, zero primary nonthumb/palm blockers, zero new severe held/core frames, zero new IK contact failures, zero new mesh-contact failures and zero new active nonthumb pair frames in affected neighbors. Primary assigned-finger key core depth is at most1.973247mm and palm key-core vertices are absent. Four finger changes preserve all1068 attacks, pitches, velocities and durations, and all40 held soprano entries.

`motion-gate.json` covers3,345 poses per score at500Hz over the four windows. Maximum tip speed decreases3.507825→3.030102m/s. Wrist speed increases1.088362→1.475801m/s. Maximum local joint rate decreases129.450320→103.604736rad/s; these still-high legacy transition rates are a review queue, not a new biological acceptance claim. Direct-seek positions and normalized quaternion components reproduce exactly at12 window samples.

Methods and limits: actual Human vertices with >65% digit/palm ownership against all88 animated rounded-key cores, and exact noncoplanar edge/triangle pairs between fully owned triangles. Core depth means closest original key-box face, not vertical depth. Actual assigned-finger skin vertices in the key's inset horizontal footprint were additionally checked for a newly floating pad above3mm. Mixed web/wrist vertices, enclosed/coplanar cases and instants between samples are not certified. Baseline idle-thumb interactions remain in raw rows and are excluded from the local nonthumb-pair acceptance; p00914 still has idle-thumb/index contact until root's runtime is combined. Smaller nonzero core contacts require visual tolerance review, and final mixed wrist mesh must be revalidated after accepted arm support.

An intermediate p00160 candidate was rejected despite zero core collisions because its pad floated5.6mm above the key. The delivered candidate corrects its true footprint contact and preserves its original release. No blanket hold shortening or point-only certification was used.

Reproduce: `node verify.mjs baseline`, `node verify.mjs score`, `node full-validate.mjs`, `node verify.mjs geometry`, `node motion-gate.mjs`. `harness.mjs` accepts DAYBREAK_RIG_MODULE for combined source checks. `optimize-grip.mjs` and GATES-low-support.md are a separate, unaccepted next batch for18 compact-thumb low support contexts; they do not modify this immutable primary score.
