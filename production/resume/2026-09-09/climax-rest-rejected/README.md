# Rejected resting-posture experiment

Parent visually rejected all candidates. Do not promote this source or use its neutral generator as an accepted preset.

Encoded baseline: pianist2599ff6f11b668711040cf1e18849bb7bcc765d3667d8505662208852a2b9e9e; score44ef4cdc314ecd2e926cca116433210c5e8b603b84856c6393fdd1f6aa462c2f. No score, physical release, pedal, wrist or active joint changes were made.

At159.3, the baseline idle LMiddle/LRing crossed in12 triangle pairs; RMiddle crossed held RRing in85 pairs; held RRing/RPinky crossed in7. Both thumbs shared a raised resting preset. Inspection of three chronological native frames for each of3 fixed neutral curves showed better curvature but introduced key penetrations and neighbor crossings.

A fourth structural prototype (`neutral-generator.ts`) retained whole phalanx curvature/roll, used mirrored thumb targets, an MCP height adjustment, and a common fan guided by held proximal directions. Its validation scope was17 full long gaps intersecting158.5–160.5 (153.403546–163.825895 total). It is reusable code, not an accepted implementation. It reduced the target-frame pair counts12→0 and85→50; the held7 remained. Parent judged its actual1920 authored-camera silhouette still contorted.

997 sample times /1994 hand states per version: 60Hz plus attack/release/fade boundaries. All1706 held samples, mesh contact gaps, wrists and outside-scope poses were exact. Native compiled source exactly matched candidate harness in6 hand states. Tests use actual moving key surfaces and individually owned mesh triangle pairs; detailed before/after records are retained, not aggregate-only acceptance.

Full gaps failed:326 worse per-key count/depth rows,242 with worse depth over3mm;847 pair-count increases, including609 newly crossing rows. Worst new core was RThumb→MIDI71,7.127780813204379mm at163.553546, vertex3117 (baseline0). The thumb tip-height floor failed to clear proximal skin. Active-neighbor fan retargeting also worsened motion: RMiddle peak tip0.834863577→1.825419459m/s, MCP8.140067869→21.487080057rad/s on the sampled gap. These are sampled extrema, not continuous maxima.

The helper's height envelope is an approximate keyboard clearance envelope; validation still uses actual moving key geometry. It is insufficient to enforce complete mesh clearance or joint ordering during transitions. No whole-piece acceptance is claimed.

Evidence: `comparison-summary.json`, `comparison-details.json.gz`, `baseline-full.json.gz`, `coordinated-full.json.gz`, candidate sources, `inspection.json`, and `native/` chronological actual rendered images/provenance. `neutral-generator.ts` is supplied only as a rejected causal prototype. Parent reopened the chord fingering rather than continuing this parameter family.
