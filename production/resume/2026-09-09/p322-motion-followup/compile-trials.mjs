import fs from'node:fs';import ts from'typescript';for(const v of JSON.parse(fs.readFileSync('trials.json'))){let js=ts.transpileModule(fs.readFileSync(v.name+'.ts','utf8'),{compilerOptions:{target:ts.ScriptTarget.ES2022,module:ts.ModuleKind.ESNext}}).outputText;js=js.replace(/from '(\.\/[^']+)'/g,(_,x)=>"from '/workspace/sites/daybreak-piano-film/production/qa/compiled/"+x.slice(2)+".mjs'");fs.writeFileSync(v.name+'.mjs',js);}
