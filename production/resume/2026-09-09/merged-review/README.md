# Parent integration review

Root merged the three small leaf deltas onto the current app, retaining the previously accepted RH Middle curve and p00170 pad adjustment. The score hash and exact current sources are in ../current.json. All final publication/master gates remain open.

The app's actual local/world bone components and full deformed Human skin match each immutable leaf reference exactly: right transfer 464 states / 5 full skins, left bass 428 / 5, left idle cutoff 209 / 3. The time sets include 500 Hz and exact event/curve boundaries ±1 microsecond. Targeted strict performance TypeScript passed. This integration check transfers the leaf evidence; it is not a new claim of continuous-time collision freedom.

Root independently recounted the 1,986-state idle repair geometry report, verified its 34 hashes, ran the RH final assertions, and restored/hash-checked/recomputed the 4,225-state bass comparison. Root viewed the three final RH native frames, four chronological bass candidate frames, and the actual merged left idle cutoff frame. No new held failure or key regression is hidden in these scoped checks.

Accepted improvement tradeoffs remain explicit: RH Index angular peak falls 144.326→28.264 rad/s, but local wrist acceleration rises 21.758→59.533 m/s². Existing Index/Middle intersections remain. The bass contact is visibly cleaner and passes the finite key/neighbor comparisons, while its MCP angular peak rises 71.634→75.192 rad/s; a bounded timing followup is not yet integrated. The index cutoff retains most prior improvement but restores a few old key contacts; see the leaf queue. The 91.474-second curve remains unresolved.

The full leaf source, measured rows, rejected candidates and native provenance are in ../leaf-evidence, indexed by ../leaf-evidence-manifest.json. The root's rejected G5 lift/refingering and p299 single-note trials are in ../rejected-root-trials. Do not turn a rejected trial into an accepted fix based solely on its held-contact numbers.
