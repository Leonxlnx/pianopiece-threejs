import fs from'node:fs';import crypto from'node:crypto';import{update,model,piano}from'./harness.mjs';const input=JSON.parse(fs.readFileSync('audit-proposed.json'));const times=input.rows.map(r=>r.time);const hashes=times.map(time=>{update(time);const data=[];model.traverse(b=>{if(b.isBone)data.push(b.name,...b.position.toArray(),...b.quaternion.toArray(),...b.matrixWorld.elements);});for(const k of piano.keys.values())data.push(k.midi,...k.mesh.matrixWorld.elements);return[time,crypto.createHash('sha256').update(JSON.stringify(data)).digest('hex')];});fs.writeFileSync(process.argv[2],JSON.stringify(hashes));console.log(hashes.length);
