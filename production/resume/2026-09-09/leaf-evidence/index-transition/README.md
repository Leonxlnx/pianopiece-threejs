This is a bounded improvement candidate, not a collision-free phrase or final-film approval.

Integrate the two note changes from `score-delta.json` into the latest score. Use `proposed-extension.ts` as the small runtime delta: it expects the existing class renamed `OriginalPianist` and exports a derived `Pianist`. Existing `THREE`, `v3`, `smooth`, `keyX`, `isBlack`, `GrandPiano`, `Note`, `Score`, `HandRig`, and `HandPose` bindings are reused. `proposed-runtime.ts` is the complete typed baked source for comparison, without load-time score mutations. `proposed-native.ts` embeds exactly those two score edits in load solely for native review. Avoid replacing a newer parent checkout with the old baked base.

The repair keeps fingering. It releases B4(p00519) at106.81 and E5(p00524) at107.18, interpolates complete local joint poses against live key surfaces, rounds idle digits with coupled MCP/PIP/DIP bends, and adds two short wrist clearance arcs. The first arc ends at the thumb's next attack; no held note is carried by the arc. Physical note-offs are85.774ms and61.383ms earlier. Existing pedal-up at106.870774 gives an intentional25ms earlier acoustic phrase release for B4; E5 still releases under sustain. Parent approved this breathing and will remaster from the final merged score.

`handoff.json` contains exact matched-sample results, SHA256 values, note/audio deltas, outside identity, native parity, and limitations. Reproduce the final assertions with `python finalize.py`. `audit-final.mjs` samples surfaces at500Hz plus boundaries and motion at2kHz. Final views are in the native report named in handoff.

Residual RIndex/RMiddle intersections remain, reduced from the baseline; wrist speed and acceleration rise while index rotation and fingertip speed fall substantially. These are disclosed integration risks, not omitted failures. The baseline's idle thumb and later pinky core events remain. Six strict TypeScript diagnostics already exist in the baked baseline; this delta adds none. Rejected experiments remain in scratch only.
