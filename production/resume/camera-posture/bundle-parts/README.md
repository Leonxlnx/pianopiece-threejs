# Exact camera review archive

The source host rejected the single 71 MiB upload with HTTP 413. These ordered parts reproduce the original archive byte for byte. Concatenate part-00 through part-05, then verify the archive SHA-256 in manifest.json before extraction. No images were discarded or recompressed.
