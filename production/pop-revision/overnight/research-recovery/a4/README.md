# Unaccepted A4 transition research recovery

This archive preserves research, not a replacement application. The accepted application is parent commit4982ad7, with12routes score90751; do not deploy any file under unaccepted. The new db00522 source/fingering experiments have not passed their final gates.

Input hints identify two independently verified Git score blobs; the13included compiled modules are exact accepted checkpoints. The integrated baseline pianist module is213745de... and includes the wrist-volume corrector exactly once. The source matching it is inputs/integrated-213745de-pianist.ts. The frozen original model must be restored separately with SHA-256be0341c8b4749721dc884eae7adfd5966dfa8db7c7f15319e4ddae49a71879bc (available from the durable pre-tailoring checkpoint; follow repository asset restoration notes). The project lockfile SHA is07490981771f4197300dfdf690643d571016d0420b1b568d951f9d2a4c9c9c8f. Use its installed TypeScript/Three dependencies.

CURRENT-STATE.md gives the accepted/rejected distinction, exact note IDs, windows, numerical controls and pending work. Qa files retain their research-relative paths deliberately; they should be staged in an isolated research workspace, never executed against application source. Before reuse, set the runtime model path to a preserved original-model copy and bind every input hash. The original local model path is only a hint, not a required artifact. Recreate candidate score files from the twelve score identified in input-hints.json by applying only the declared experimental controls. No score copy for each rejected profile is necessary.

Reconstruction: approach-research/baseline is the twelve score with frozen compiled modules; per-note grids mutate only db00522.approachPose. Approach-waypoint source adds an explicit opt-in arrival; candidate notes set db00522.approachWaypoint (recorded profiles). Approach-offset only changes lateral return timing; approach-idle additionally gives an explicitly named resting digit a joint-space offset. Paired candidate uses middle arrival and db00518 index releaseWaypoint or idlePose. Every such combination failed at least one geometry, motion or shape screen.

Fingering research changes db00522 frommiddle toindex, keeping every audible field exact. The same-depth variant retains contactZ0.249 and has a clear sampled hold, but its transitions still regress. Its approach/release grids are active at archive creation; evidence files ending checkpoint may be incomplete and cannot establish acceptance. Recreate same score by changing only db00522.finger to2 in the twelve score. Its expanded window is115.489354 through116.793534, covering prior release, the new A4 hold, and the following G4 hold. The joint-space/fingering grids are discovery only. A candidate must still pass independent full-window240Hz actual-skin surfaces,960Hz motion, actual assigned-pad checks, held/endpoints/unrelated8digits/arms, and native visual review.

Full accepted-score60Hz inventory:13628samples,246severe idle-key patch rows,1386neighbor-positive frames, no active-key/palm/opposing rows. Compact event summaries retain notes/times, while raw triangle dumps are omitted from this archive. The attempted full12-score scan was rejected because the mutable model changed during execution; it is not evidence.

All surface parameter samplers were compared against an independent CLI baseline, and completed grids check A-to-B-to-A exact state restoration. Quick grid tip measurements exclude sub1/960second duplicate-neighbor intervals around exact note boundaries; independent uniform motion remains the acceptance oracle. Thresholds were not weakened.
