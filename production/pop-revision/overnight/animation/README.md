# Recoverable animation checkpoint

Replace only the performance `pianist.ts`, `types.ts`, and fitted `public/assets/score.json` with the three files in this folder. Preserve root's room, direction, piano, lighting, clothing and other current implementation files. Verify the exact hashes in `manifest.json`, recompile, and run root's final combined240Hz audit. This is a scoped repair checkpoint, not a completed film or collision-free hand claim.

The source is exact recovered V7 plus the reviewed explicit finite-release waypoint branch. The score includes the A4pair, seven recovered release profiles, and two15° release controls on db00493/db00757. It preserves all fitted and audible fields of the saved V7/E3 score. The source/type files and score are sufficient to reconstruct these changes if scratch disappears.

Sparse deltas retain each expected full note before replacement. The A4 source patch is relative to exact V7; the full source files avoid requiring older local patches. The unchanged model is recoverable through the existing verified asset archive.

A4: eight severe right-index/B-flat-key samples clear. Seven later routes: all3,956 targeted neighbor triangle-pair incidences clear in the finite windows; two further severe key samples clear. These counts are finite intersection incidences, not distinct collisions or a penetration-depth measurement. Existing unrelated failures remain. Two A4 and six seven-route shallow key rows remain below3mm and are reported.

Short gaps: local240Hz peak fingertip speed5.274669→4.824965m/s;960Hz peak4.889783m/s. Key contacts increase at no measured depth;236 neighbor triangle-pair incidences disappear. Held and endpoint skin, unrelated joints and audible fields are exact. The no-arch candidate was rejected because it hit keys3.81/6.53mm deep.

The two A4 and seven recovered-route still families were inspected against their exact source/score hashes. Four short-gap stills and root's final combined full-song audit are pending. Root's earlier full-song A4 audit passed all5,055 held-skin samples but revealed the two speed failures addressed here; that older audit must not be relabeled as a pass on this new score.

Local verifier helpers were corrected to compute acceleration between velocity midpoints when a guard endpoint produces an unequal final time step. This did not change thresholds. The A4 gate was rerun; seven and short-gap numerical reports use the corrected helper.
